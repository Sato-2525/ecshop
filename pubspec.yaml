name: ecshop_techpit
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0-302.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_lints: ^2.0.3
  google_fonts: ^5.1.0
  flutter_riverpod: ^2.4.0
  freezed: ^2.4.2
  freezed_annotation: ^2.4.1
  go_router: ^10.1.2

dev_dependencies:
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/mock.json
